// export default (a, b) => a + b;

// const add = (a, b) => a + b;
// const subtract = (a, b) => a - b;

// export default {
//   add,
//   subtract,
// };

// export default add;

// named exports
export const add = (a, b) => a + b;
export const subtract = (a, b) => a - b;
